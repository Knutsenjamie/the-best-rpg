const sergeantsOrders = character => 
  character.type === `orc` 
    ? `attack that orc!` 
    : `let them be`

const double = (n) => (n * 2)
const triple = (n) => {
  return n * 3
}
function quadruple(n) {
  return n * 4
}